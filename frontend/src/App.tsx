import { Container } from "@mantine/core";
import Form from "./ui/Form";

const App = () => {
  return (
    <div>
      <Container size="xl">
        <h1>Fast start formatter (stow only)</h1>
        <Form />
      </Container>
    </div>
  );
};

export default App;
